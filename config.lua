Config = {}
Config.Inventory = 'qb' -- qb or ox. nothing else will work
Config.Debug = false
Config.Sundown = false -- You probably want to set this to false

Config.Items = { -- if you want to change the item names do it here
    notepad = 'cwnotepad',
    note = 'cwnote'
}
